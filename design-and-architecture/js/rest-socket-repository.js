/**
 * Task:
 * We have a rest with 100 endpoint
 * How we should implement it?
 * What will we do if we have now to give this endpoint with socket.io
 *
 * Answer:
 * We should use repository pattern
 */

function f() {

}