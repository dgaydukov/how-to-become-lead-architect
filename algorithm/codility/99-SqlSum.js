/**
 * This is not even js question, so it's unclear how it got here, but anyway we will solve it, cause it just very simple query
 * SELECT sum(v) from elements
 *
 * https://app.codility.com/demo/results/trainingXMJXEB-AEP
 */
